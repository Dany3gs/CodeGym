package Ejercicios.Tema9;

/*En la clase Solution, reemplace todos los nombres de clase completos en el código por nombres cortos.

Requisitos:
•	El método main debe contener sólo nombres de clase cortos: el resto no se puede cambiar.
•	Debe haber 3 importaciones en la clase Solution*/

import java.io.InputStreamReader;
import java.util.LinkedList;
import java.util.concurrent.atomic.AtomicBoolean;

public class ImportacionesParte2 {
    public static void main(String[] args) {
        LinkedList<String> linkedList = new LinkedList<>();
        InputStreamReader inputStreamReader = new InputStreamReader(System.in);
        AtomicBoolean atomicBoolean = new AtomicBoolean(false);
    }
}
//ok Codegym